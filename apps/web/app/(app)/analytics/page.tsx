'use client';import{useQuery}from'@tanstack/react-query';import{api}from'../../../lib/api';import{BarChart,Bar,ResponsiveContainer,XAxis,YAxis,Tooltip}from'recharts';type S={ranking:Array<{id:string;title:string;_count:{reports:number}}>};export default function Analytics(){const{data}=useQuery({queryKey:['summary'],queryFn:()=>api<S>('/analytics/summary')});const rows=data?.ranking.map(x=>({name:x.title,uses:x._count.reports}))??[];return <><div className="mb-7"><p className="text-sm text-muted">Performance</p><h1 className="text-3xl font-semibold">Analytics</h1></div><section className="card h-80 p-5"><h2 className="mb-4 font-semibold">Top content by detected use</h2><ResponsiveContainer width="100%" height="88%"><BarChart data={rows}><XAxis dataKey="name" hide/><YAxis/><Tooltip/><Bar dataKey="uses" fill="hsl(var(--brand))" radius={[5,5,0,0]}/></BarChart></ResponsiveContainer></section></>}
