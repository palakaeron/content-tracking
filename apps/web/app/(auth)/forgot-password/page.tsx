import Link from'next/link';export default function Forgot(){return <main className="mx-auto flex min-h-screen max-w-sm items-center p-5"><div className="card p-6"><h1 className="text-2xl font-semibold">Reset your password</h1><p className="mt-3 text-sm text-muted">Password reset email issuance is intentionally disabled until a transactional email provider is configured. The backend schema supports single-use, expiring tokens.</p><Link className="mt-5 block text-sm text-brand" href="/login">Back to sign in</Link></div></main>}
