import bcrypt from 'bcrypt';import {PrismaClient}from'@prisma/client';const db=new PrismaClient();async function main(){const user=await db.user.upsert({where:{email:'demo@sentinel.local'},update:{},create:{email:'demo@sentinel.local',name:'Demo Creator',passwordHash:await bcrypt.hash('DemoPassword123!',12),emailVerifiedAt:new Date()}});const content=await db.content.createManyAndReturn({data:[{ownerId:user.id,title:'Coastal light study',type:'IMAGE',status:'AVAILABLE'},{ownerId:user.id,title:'A note on creative ownership',type:'TEXT',textBody:'Original writing about rights and attribution.',status:'AVAILABLE'},{ownerId:user.id,title:'Studio reel',type:'VIDEO',status:'AVAILABLE'}]});const report=await db.report.create({data:{contentId:content[0]!.id,sourceUrl:'https://example-source.test/coastal-light',confidence:.91,matchType:'Visual similarity'}});await db.alert.create({data:{ownerId:user.id,reportId:report.id,severity:'HIGH'}})}main().finally(()=>db.$disconnect());
