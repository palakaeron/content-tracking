import { Router } from 'express'; import rateLimit from 'express-rate-limit'; import { authController,contentController,analyticsController,alertController } from '../controllers/controllers.js'; import { requireAuth,validate,csrf } from '../middleware/core.js'; import { signupSchema,loginSchema,contentCreateSchema,contentUpdateSchema,paginationSchema,alertUpdateSchema } from '@cut/shared'; import { z } from 'zod'; import { asyncHandler } from '../utils/errors.js';
const r=Router(),auth=Router();const wrap=(f:Parameters<typeof asyncHandler>[0])=>asyncHandler(f);const id=z.object({id:z.string().uuid()}); const limiter=rateLimit({windowMs:15*60e3,max:10,standardHeaders:true,legacyHeaders:false});
auth.post('/signup',limiter,validate(signupSchema),wrap(authController.signup));auth.post('/login',limiter,validate(loginSchema),wrap(authController.login));auth.post('/refresh',wrap(authController.refresh));auth.post('/logout',csrf,wrap(authController.logout));r.use('/auth',auth);
r.use(requireAuth);r.get('/content',validate(paginationSchema,'query'),wrap(contentController.list));r.post('/content',csrf,validate(contentCreateSchema),wrap(contentController.create));r.get('/content/:id',validate(id,'params'),wrap(contentController.get));r.patch('/content/:id',csrf,validate(id,'params'),validate(contentUpdateSchema),wrap(contentController.update));r.delete('/content/:id',csrf,validate(id,'params'),wrap(contentController.remove));r.post('/content/:id/scan',csrf,validate(id,'params'),wrap(contentController.scan));r.get('/analytics/summary',wrap(analyticsController.summary));r.get('/alerts',wrap(alertController.list));r.patch('/alerts/:id',csrf,validate(id,'params'),validate(alertUpdateSchema),wrap(alertController.update));export default r;
